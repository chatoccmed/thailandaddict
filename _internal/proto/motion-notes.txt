Restrained motion module for astro/public/index.html — additive only, zero brand-color/font changes, transform/opacity exclusively. Ships exactly four effects: (1) hero .hero-h1 mask-reveal — splits the h1 into its two logical Thai tokens by top-level child node (text "ชีวิต" + the <em>ติดเที่ยว</em>), each wrapped in an overflow:hidden mask with a .ta-word that wipes up from translateY(110%), staggered 90ms, .9s cubic-bezier(.16,1,.3,1). The <em> element is cloned intact so its yellow accent (#FFE15D) + text-shadow overrides survive; the mask gets padding-bottom/negative-margin so Thai lower vowels/tone marks aren't sheared. (2) Scroll-reveal fade + 20px rise (no scale) on .sec,.s-head,.rgcard,.tcard,.hcard,.grescard,.pcard,.stat via IntersectionObserver, one-shot, with a per-grid --i stagger (70ms, capped at 8). Elements already in the viewport on load reveal immediately (no scroll needed). (3) Count-up on .stat .n — parses "2,200+", "4,000+", "100%", "77" keeping prefix/suffix + thousands comma, 1.3s ease-out cubic, fired when the .stats band scrolls in. Note: the static 100% cell has no data-stat but is still animated (selector is .stat .n, text-parsed). (4) Slim brand-gradient scroll-progress bar (#ta-progress) fixed at top:0, rAF-throttled.

CRITICAL bug guard: .tcard and other cards are JS-injected after load — handled three ways: MutationObserver on the known generator containers (#topnscroll/#destscroll/#hotelgrid/#artgrid/#tier1/#tier2/#onerail/.hscroll/.rggrid/.gres + main/body fallback), plus 400ms and 1400ms re-sweeps. Newly injected cards get .ta-rv + observed so they reveal too.

prefers-reduced-motion: full kill switch in both CSS (@media block neutralizes masks/reveal/progress) and JS (skips hero split, progress bar, and all animation — count-up renders final values immediately, reveals show at rest). No tilt, custom cursor, nav-hide, or magnetic buttons. Does not touch the existing ken-burns (@keyframes kz) or the inert legacy .rv class.

Integration: paste the CSS into a <style> block and the JS into a <script> before </body> (idempotent via data-* guards; safe if run twice). It does not use or collide with the site's existing .rv/rvSafety reveal system.